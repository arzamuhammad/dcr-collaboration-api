INSERT INTO DCR_POC_1M.PROVIDER_DATA.C360_TELCO_BACKUP
SELECT
  'CUST_' || LPAD(SEQ8()::STRING, 12, '0') AS CUSTOMER_ID,
  SHA2('628' || LPAD(SEQ8()::STRING, 10, '0'), 256) AS HASHED_MSISDN,
  SHA2('user' || SEQ8()::STRING || '@telco.co.id', 256) AS HASHED_EMAIL,
  18 + UNIFORM(0, 60, RANDOM()) AS AGE,
  DECODE(UNIFORM(0,1,RANDOM()),0,'M','F') AS GENDER,
  ARRAY_CONSTRUCT('DKI Jakarta','Jawa Barat','Jawa Timur','Jawa Tengah','Banten','Sumatera Utara','Sumatera Selatan','Sulawesi Selatan','Bali','Kalimantan Timur')[UNIFORM(0,9,RANDOM())]::STRING AS PROVINCE,
  ARRAY_CONSTRUCT('Jakarta','Bandung','Surabaya','Medan','Semarang','Makassar','Palembang','Denpasar','Balikpapan','Yogyakarta')[UNIFORM(0,9,RANDOM())]::STRING AS CITY,
  ARRAY_CONSTRUCT('<3M','3-5M','5-10M','10-20M','>20M')[UNIFORM(0,4,RANDOM())]::STRING AS INCOME_BRACKET,
  ARRAY_CONSTRUCT('Student','Employee','Self-Employed','Professional','Entrepreneur','Retired')[UNIFORM(0,5,RANDOM())]::STRING AS OCCUPATION,
  ARRAY_CONSTRUCT('SMP','SMA','D3','S1','S2','S3')[UNIFORM(0,5,RANDOM())]::STRING AS EDUCATION,
  ARRAY_CONSTRUCT('Single','Married','Divorced')[UNIFORM(0,2,RANDOM())]::STRING AS MARITAL_STATUS,
  1 + UNIFORM(0, 7, RANDOM()) AS FAMILY_SIZE,
  ARRAY_CONSTRUCT('PREPAID','POSTPAID')[UNIFORM(0,1,RANDOM())]::STRING AS PLAN_TYPE,
  ARRAY_CONSTRUCT('Basic','Silver','Gold','Platinum','Unlimited')[UNIFORM(0,4,RANDOM())]::STRING AS PLAN_NAME,
  ROUND(UNIFORM(20000, 800000, RANDOM())::FLOAT, 2) AS MONTHLY_ARPU,
  DATEADD('day', -UNIFORM(0, 1460, RANDOM()), CURRENT_DATE()) AS CONTRACT_START_DATE,
  DATEADD('day', UNIFORM(0, 730, RANDOM()), CURRENT_DATE()) AS CONTRACT_END_DATE,
  ARRAY_CONSTRUCT('Autodebit','Ewallet','Bank Transfer','Credit Card','Retail')[UNIFORM(0,4,RANDOM())]::STRING AS PAYMENT_METHOD,
  ARRAY_CONSTRUCT('Monthly','Quarterly','Annual')[UNIFORM(0,2,RANDOM())]::STRING AS BILLING_CYCLE,
  ARRAY_CONSTRUCT('Bronze','Silver','Gold','Platinum','Diamond')[UNIFORM(0,4,RANDOM())]::STRING AS LOYALTY_TIER,
  UNIFORM(1, 120, RANDOM()) AS TENURE_MONTHS,
  ARRAY_CONSTRUCT('Samsung','Apple','Xiaomi','Oppo','Vivo','Realme','Huawei','Infinix')[UNIFORM(0,7,RANDOM())]::STRING AS DEVICE_BRAND,
  'Model-' || UNIFORM(1,500,RANDOM())::STRING AS DEVICE_MODEL,
  DECODE(UNIFORM(0,1,RANDOM()),0,'Android','iOS') AS DEVICE_OS,
  UNIFORM(8,17,RANDOM())::STRING || '.' || UNIFORM(0,9,RANDOM())::STRING AS OS_VERSION,
  ARRAY_CONSTRUCT(3,4,6,8,12,16)[UNIFORM(0,5,RANDOM())]::NUMBER AS DEVICE_RAM_GB,
  UNIFORM(0,1,RANDOM())=1 AS IS_5G_CAPABLE,
  ARRAY_CONSTRUCT('Physical','eSIM','Dual')[UNIFORM(0,2,RANDOM())]::STRING AS SIM_TYPE,
  UNIFORM(0, 2000, RANDOM()) AS VOICE_MIN_M1,
  UNIFORM(0, 2000, RANDOM()) AS VOICE_MIN_M2,
  UNIFORM(0, 2000, RANDOM()) AS VOICE_MIN_M3,
  UNIFORM(0, 100000, RANDOM()) AS DATA_MB_M1,
  UNIFORM(0, 100000, RANDOM()) AS DATA_MB_M2,
  UNIFORM(0, 100000, RANDOM()) AS DATA_MB_M3,
  UNIFORM(0, 500, RANDOM()) AS SMS_M1,
  UNIFORM(0, 500, RANDOM()) AS SMS_M2,
  UNIFORM(0, 500, RANDOM()) AS SMS_M3,
  UNIFORM(0, 5000, RANDOM()) AS ROAMING_MB_M1,
  ROUND(UNIFORM(0, 100, RANDOM())/100.0, 4) AS CHURN_SCORE,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS CLV_SCORE,
  ROUND(UNIFORM(0, 100, RANDOM())/100.0, 4) AS NBA_SCORE,
  UNIFORM(300, 850, RANDOM()) AS CREDIT_SCORE,
  ROUND(UNIFORM(0, 100, RANDOM())/100.0, 4) AS ENGAGEMENT_SCORE,
  UNIFORM(0,1,RANDOM())=1 AS IS_HIGH_VALUE,
  UNIFORM(0,9,RANDOM())=0 AS IS_ROAMER,
  UNIFORM(0,1,RANDOM())=1 AS IS_FAMILY_PLAN,
  UNIFORM(0,1,RANDOM())=1 AS OPT_IN_SMS,
  UNIFORM(0,1,RANDOM())=1 AS OPT_IN_EMAIL,
  UNIFORM(0,1,RANDOM())=1 AS OPT_IN_PUSH,
  DATEADD('day', -UNIFORM(0, 90, RANDOM()), CURRENT_DATE()) AS LAST_ACTIVITY_DATE,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0001,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0002,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0003,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0004,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0005,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0006,
  UNIFORM(0,1,RANDOM())=1 AS FEAT_0007,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0008,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0009,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0010,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0011,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0012,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0013,
  UNIFORM(0,1,RANDOM())=1 AS FEAT_0014,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0015,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0016,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0017,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0018,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0019,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0020,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0021,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0022,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0023,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0024,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0025,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0026,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0027,
  UNIFORM(0,1,RANDOM())=1 AS FEAT_0028,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0029,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0030,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0031,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0032,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0033,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0034,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0035,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0036,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0037,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0038,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0039,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0040,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0041,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0042,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0043,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0044,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0045,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0046,
  ROUND(UNIFORM(0, 1000, RANDOM())/10.0, 2) AS FEAT_0047,
  UNIFORM(0, 1000, RANDOM()) AS FEAT_0048,
  UNIFORM(0,1,RANDOM())=1 AS FEAT_0049,
  ROUND(UNIFORM(0, 10000, RANDOM())/100.0, 2) AS FEAT_0050
FROM TABLE(GENERATOR(ROWCOUNT => 1000000));